# CUMCM 2026 数学建模论文 Skills V4-RC1

> 状态：Release Candidate 1。已完成近三年 10 篇优秀 A 题论文的逐篇学习，并经过“华数杯微构体 A–A 最短距离”多轮绘图实测修订。
>
> 用途：2026 全国大学生数学建模竞赛（CUMCM）赛题分析、数学模型建立、证明、数值求解、后续问题递进、最后一问升级、论文写作与科研绘图。
>
> 最高优先级：**结构正确性 > 数学可解释性 > 证明与验证 > 图形论证 > 算法复杂度 > 视觉装饰。**

---

# 0. 总工作链

任何题目默认按下列链条推进：

\[
\boxed{
\text{读题拆解}
\rightarrow
\text{问题依赖}
\rightarrow
\text{现实/几何抽象}
\rightarrow
\text{基础状态模型}
\rightarrow
\text{结构性质证明}
\rightarrow
\text{数值求解}
\rightarrow
\text{独立验证}
\rightarrow
\text{图形解释}
\rightarrow
\text{后续问题继承与升级}
}
\]

禁止：

- 没有机理模型就套算法；
- 每一问重新建一套互不相关的模型；
- 未判断函数结构就直接调用 PSO/GA/DE；
- 把“程序跑通”当成“理论正确”；
- 把绘图当作算完后的装饰；
- 用生成式 AI 伪造定量图和精确几何；
- 为了好看牺牲真实尺寸、坐标或几何关系。

---

# 1. 读题：六元组

每一问先拆成：

\[
\mathcal Q=(O,S,R,C,J,Y)
\]

- \(O\)：对象 Object
- \(S\)：状态 State
- \(R\)：关系/机理 Relation
- \(C\)：约束 Constraint
- \(J\)：目标 Objective
- \(Y\)：输出 Output

必须回答：

1. 已知量是什么？
2. 未知状态是什么？
3. 决策变量是什么？
4. 中间变量是什么？
5. 哪些量随时间/空间/对象编号变化？
6. 哪些约束来自物理？
7. 哪些约束来自题面？
8. 哪些结果会成为下一问输入？

---

# 2. 先确定各问继承关系

优先画出：

```text
Q1 基础状态模型
│
├─ Q2 = Q1 + 新判据 / 新约束 / 临界事件
│
├─ Q3 = Q1/Q2 + 参数反求 / 优化变量
│
├─ Q4 = 分段 / 多状态 / 多对象
│
└─ Final = 全局化 + 降维 + 分解 + 优化 + 稳健性
```

第一问的真正目标通常不是“得到第一个数”，而是建立：

\[
\boxed{\mathbf x = F(t,\boldsymbol\theta)}
\]

这样的**可复用基础状态计算器**。

---

# 3. 第一问：基础模型

标准顺序：

1. 选择参考系/坐标系；
2. 画最小机理图；
3. 定义变量；
4. 从几何/物理关系推导状态方程；
5. 处理多根、多解和分支；
6. 给出数值求解；
7. 用独立路线验证。

示例：

\[
\mathbf r(\theta)=
\begin{bmatrix}
x(\theta)\\y(\theta)
\end{bmatrix}
\]

\[
s(\theta)=
\int_{\theta_0}^{\theta}
\left\|
\frac{d\mathbf r}{d\xi}
\right\|d\xi
\]

\[
v=\frac{ds}{dt}
=
\left\|
\frac{d\mathbf r}{d\theta}
\right\|\dot\theta
\]

---

# 4. 证明：先证明结构，再算数值

优先寻找并证明：

1. 单调性；
2. 单峰性；
3. 对称性；
4. 周期性；
5. 局部性；
6. 最近邻结构；
7. 连续性；
8. 凸性或近凸性；
9. 比例/尺度关系；
10. 守恒关系；
11. 边界发生条件；
12. 可分解结构。

目标是把：

\[
O(n^2)
\]

之类的暴力问题，通过数学结构降成：

\[
O(kn),\quad k\ll n
\]

或把高维优化分解成多个低维问题。

---

# 5. 六层正确性体系

## L1 量纲

检查公式量纲闭合。

## L2 几何/物理机理

公式应能由几何、物理或统计机理推导。

## L3 结构性质

证明算法为什么可以降维或采用某类搜索。

## L4 算法适用性

二分必须有单调/符号区间；三分必须有单峰性。

## L5 独立数值验证

主程序之外再走一条计算路线。

## L6 稳健性/敏感性

例如：

\[
S_i=
\frac{\Delta y/y}{\Delta p_i/p_i}
\]

---

# 6. 后续问题如何继承

## Q2：基础状态 + 新判据

若第一问：

\[
\mathbf x=F(t,\theta)
\]

第二问通常可写成：

\[
g(\mathbf x)\le0
\]

临界事件：

\[
t^*=
\inf\{t:g(\mathbf x(t))\le0\}.
\]

---

## Q3：基础模型 + 参数反求/优化

\[
\min_\theta J(\theta)
\]

s.t.

\[
g(F(t,\theta))\ge0.
\]

代码结构应为：

```text
base_state()
    ↓
constraint_check()
    ↓
objective()
    ↓
optimizer()
```

---

## Q4：多阶段/分段模型

\[
\mathbf x(t)=
\begin{cases}
F_1(t),&t<t_1\\
F_2(t),&t_1\le t<t_2\\
\cdots
\end{cases}
\]

检查：

\[
\mathbf x(t_k^-)=\mathbf x(t_k^+)
\]

必要时：

\[
\dot{\mathbf x}(t_k^-)=\dot{\mathbf x}(t_k^+).
\]

---

# 7. 最后一问

禁止一上来就使用全局智能优化。

固定顺序：

1. 画完整结构；
2. 找对称性；
3. 找局部性；
4. 找可分区/可分阶段结构；
5. 解析消元；
6. 找比例/尺度关系；
7. 子问题分解；
8. 再决定优化器；
9. 最后做稳健性。

典型混合优化：

\[
\max_{\mathbf z,\mathbf x}J(\mathbf z,\mathbf x)
\]

其中 \(\mathbf z\) 为离散变量，\(\mathbf x\) 为连续变量。

---

# 8. 算法选择

## 二分

用于单调阈值：

\[
f(a)f(b)<0.
\]

## 三分/黄金分割

用于一维单峰。

## 粗搜→细搜

先找区域，再提高精度。

## 递推

用于：

\[
x_i=F(x_{i-1}).
\]

## Monte Carlo

用于概率/高维积分：

\[
\hat p_N=\frac1N\sum_{i=1}^N I_i.
\]

## PSO/GA/DE

只在多维、非凸、多峰、导数困难、约束复杂时考虑。

**算法名称不能代替数学模型。**

---

# 9. 论文表达递进

每一个模型章节按：

\[
\boxed{
\text{观察}
\rightarrow
\text{抽象}
\rightarrow
\text{定义}
\rightarrow
\text{推导}
\rightarrow
\text{性质}
\rightarrow
\text{求解}
\rightarrow
\text{验证}
\rightarrow
\text{解释}
\rightarrow
\text{延伸}
}
\]

图、公式、文字应能互相翻译。

---

# 10. 绘图最高规则

绘图详细规则见：

`drawing/FIGURE_SKILL.md`

核心纪律：

1. **一张图就是一张图。默认禁止拼图。**
2. 需要三维和二维时，输出为两张独立图片，而不是 (a)(b) 拼接。
3. 默认纯白背景 + 黑色线条 + 黑色文字。
4. 不使用灰色实体填充、渐变、纹理、阴影装饰。
5. 图内只放数学对象、变量、坐标和必要尺寸。
6. 能在正文解释的内容，一律不塞进图片。
7. 每张图正下方单独配一句简短图注。
8. 三维图先展示研究域、坐标系、边界和空间结构。
9. 二维图负责局部算法、投影和数学判据。
10. 三维坐标系原点必须主动设计，不能用绘图库默认位置。
11. 规则立方体问题默认优先把原点放在透视上较“深”的三棱交点，\(x,y,z\) 沿三条棱展开，箭头延伸超过研究域。
12. 所有影响空间理解的宏观尺寸必须按题面真实比例绘制。
13. 尺寸正确首先靠图形比例体现，不靠堆数字证明。
14. 重要定量图只能由真实数据/程序生成。
15. 生成式图片只适合作为概念草图；尺寸敏感、坐标敏感图必须程序化生成或严格复核。

---

# 11. 图在论文里的任务分类

- 问题依赖图
- 坐标系/全局结构图
- 机理图
- 局部二维几何图
- 证明/降维图
- 算法流程图
- 目标函数形状图
- 结果图
- 验证/误差图
- 敏感性图
- 热力图/空间分布图
- 全局空间图
- 临界局部图

---

# 12. 图前与图后

图前回答：

> 为什么需要这张图？

图后回答：

> 从图中得到什么数学信息？如何影响下一步？

禁止只写：

> “结果如图所示。”

---

# 13. 2026 国赛现场执行顺序

## T0
六元组 + 各问依赖。

## T1
第一问建立可复用基础模型。

## T2
第二问只增加新的事件/约束。

## T3
第三问先画目标形状，再选算法。

## T4
第四问识别分段、多状态和连续性。

## T5
最后一问先降维、分解，再优化。

---

# 14. 强制验收

正式进论文前，每个核心模型必须回答：

- 模型为什么这样建？
- 公式从哪里来？
- 有什么可证明结构？
- 算法为什么适合？
- 如何独立验证？
- 后一问继承了什么？
- 图形新增了什么数学信息？

任何一个回答不清楚，都不得视为定稿。
